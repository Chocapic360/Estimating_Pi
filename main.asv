% Calculating Pi main file
clear, clc

N = 1:1000;
Nsize = size(N);
Ns = Nsize(1,2);

archimedes_pi = zeros(Ns);
archimedes_time = zeros(Ns);
archimedes_error = zeros(Ns);

for i = 1:Ns
    archimedes_vec = my_archimedes(N(i));
    archimedes_pi = archimedes_vec(i,1);
    ar
    archimedes_time = archimedes_vec(i,3);
end
